# NO SUS — Monad Experiment: Flutter app

Full NO SUS app copied from `no_sus` at
`ec29fdae94a68fefcb5efea2797224dc2245549c`, explicitly requested by the owner.
The root AGENTS.md still applies. Production files are not edited.

- Preserve the five tabs and existing app features and deep-link formats.
- Add Monad under `lib/features/monad/`, reached from Welcome and Workspace.
  Use the existing Flutter theme, accessibility and navigation.
- Backend: **ffidvfguojalzpclipzi** only. Never apply this fork's SQL to
  production `rxfnazmusofikwaggntb`. No production users or storage are copied.
- Target: **monad.nosus.foo**, web only. Native source is retained for parity;
  do not release it with the copied production application/signing identity.
- No secrets were copied. The publishable Supabase key is a public client key.
- Gate T1 still blocks Monad encrypted sharing and public release. Reading
  `canDecrypt` alone is not proof of successful Lit decryption.
- Run `flutter analyze`, `flutter test`, and `flutter build web --base-href /`.
  SDK: `C:/Users/shubh/AppData/Local/flutter/bin/flutter.bat`.
- Do not format unrelated copied Dart files. Migrations remain immutable;
  experiment-specific changes are append-only.
