<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting, using the
         AndroidX SplashScreen compat API (androidx.core:core-splashscreen) so behavior
         is consistent across API levels instead of relying only on windowBackground —
         on API 31+ this maps to the real platform SplashScreen; below that, the compat
         library reproduces the same look via this theme's attributes. -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#F5F5F3</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/launcher_icon</item>
        <!-- Theme applied immediately after the splash screen is dismissed,
             i.e. exactly what NormalTheme below already provides. -->
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
